---
description: Print the URL where ClaudeCadence is serving the timeline
---

Show the local URL where the ClaudeCadence timeline viewer is served.

Run:

```bash
echo "Open in browser: http://localhost:4173/"
echo "If you're remote (Tailscale / SSH tunnel), forward port 4173 first."
echo ""
echo "Not started yet? Run /claudecadence:serve"
```
