{
  "name": "claudecadence-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test harness for the ClaudeCadence plugin. The plugin itself (under plugins/claudecadence/) has zero runtime dependencies — this file is for the test runner only.",
  "scripts": {
    "test": "node test/run.js"
  },
  "license": "MIT"
}
